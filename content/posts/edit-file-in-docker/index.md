---
aliases:
  - '/edit-file-in-docker/'
categories: ["HowTo"]
cover:
  alt: howto
  caption: 'Illustrated by [Igan Pol](https://www.behance.net/iganpol)'
  image: howto.png
  relative: false
date: "2016-08-01T15:00:28+03:00"
lastmod: "2026-08-09T12:00:00+03:00"
tags:
- docker
- linux
- containerization
- docker exec
- docker ps
- command line
- troubleshooting
- file editing
- bash
- gitlab
title: 'Как зайти в Docker-контейнер и выполнить команду внутри'
type: post
description: "Как зайти в запущенный Docker-контейнер через docker exec, выполнить команду или отредактировать файл внутри: docker ps, docker exec -it, вход под root и грабли."
keywords: ["как зайти в контейнер docker", "docker зайти в контейнер", "docker exec", "docker exec -it", "docker ps", "редактировать файл в docker контейнере", "войти в контейнер docker", "зайти в docker", "docker exec под root", "alpine docker exec sh", "troubleshooting docker", "gitlab"]
---

Привет, `%username%`! Я с недавних пор начал изучать контейнеризацию на базе Docker. И вот в определенный момент мне понадобилось подправить пару файлов внутри готового и запущенного контейнера `gitlab`. Оказалось это делается довольно просто.

> **🔄 Обновлено 2026-08-09.** Добавил шпаргалку, варианты для Alpine (`sh` вместо `bash`), вход под root, раздел [«Как править файл, если внутри нет редактора»](#как-править-файл-если-внутри-нет-редактора) про `docker cp`, разбор [`exec` против `attach`](#docker-exec-или-docker-attach) и [тонкие места](#тонкие-места-на-которых-легко-споткнуться) про сохранение правок. Всё актуально для Docker 20.10+ и Compose v2.

Для того, чтобы выполнять команды внутри контейнера, надо для начала в него попасть. Для моих нужд достаточно одной простой команды. Но перед тем как попасть в контейнер, нам необходимо получить `ID` необходимого контейнера. Делается это командой:

```bash
docker ps
```

После получения списка всех запущенных контейнеров, мы выбираем (в первом столбце) `ID` нужного нам контейнера и подставляем в следующую команду вместо `<container_id>`:

```bash
docker exec -it <container_id> bash
```

Данная команда в контейнере `<container_id>` запустит `bash` прямо в вашей консоли. После чего, можно выполнять внутри контейнера все правки, которые нам необходимы.

## Что с этим делать на практике

Короткий набор команд под частые ситуации:

```bash
# список запущенных контейнеров (берём ID или имя из вывода)
docker ps

# зайти внутрь интерактивной оболочкой
docker exec -it <container_id> bash

# если в образе нет bash (Alpine и прочие минимальные образы) — есть sh
docker exec -it <container_id> sh

# можно обращаться по имени контейнера, а не по ID
docker exec -it gitlab bash

# выполнить одну команду, не заходя внутрь
docker exec <container_id> cat /etc/nginx/nginx.conf

# зайти под root, если внутри непривилегированный пользователь
docker exec -u 0 -it <container_id> bash
```

Флаги `-i` и `-t` почти всегда идут вместе: `-i` держит STDIN открытым, `-t` подсовывает псевдо-терминал. Без них `docker exec` отработает команду и сразу выйдет — это как раз то, что нужно для разовых команд, и не нужно для интерактивной сессии.

Если только начинаешь с Docker — загляни в [основы: Dockerfile и docker-compose](/posts/docker-base/).

## Как править файл, если внутри нет редактора

Самая частая засада: зашёл в контейнер, а там ни `vi`, ни `nano`, ни `vim`. В минимальных образах их и не должно быть — редактор в проде не нужен, он только раздувает образ. Ставить пакет внутрь работающего контейнера — плохая идея: изменение всё равно не переживёт пересоздание, а образ ты этим не починишь.

Правильный способ — вытащить файл на хост, поправить привычным редактором и положить обратно:

```bash
# вытащить файл из контейнера на хост
docker cp <container_id>:/etc/nginx/nginx.conf ./nginx.conf

# поправить чем угодно
vim ./nginx.conf

# положить обратно
docker cp ./nginx.conf <container_id>:/etc/nginx/nginx.conf
```

`docker cp` работает в обе стороны и не требует, чтобы контейнер был запущен, — с остановленным тоже. Копировать можно и директории целиком.

После подмены конфига процесс внутри обычно надо перечитать конфигурацию. Перезапускать весь контейнер не всегда нужно:

```bash
# nginx умеет перечитать конфиг без рестарта
docker exec <container_id> nginx -s reload

# если такого механизма нет — рестарт контейнера
docker restart <container_id>
```

Если редактор всё же нужен именно внутри (например, надо поковыряться в чужом образе на своей машине), проще запустить временный контейнер с примонтированным томом:

```bash
docker run --rm -it -v myvolume:/data alpine sh -c "apk add --no-cache vim && vim /data/config.yml"
```

Только помни, что для боевого сервиса это отладка, а не способ вносить изменения.

## docker exec или docker attach

Эти две команды часто путают, и путаница дорого стоит.

`docker exec` запускает **новый** процесс внутри контейнера. Вышел из него через `exit` — контейнер продолжает работать как ни в чём не бывало. Это то, что нужно в 99% случаев.

`docker attach` подключается к **основному** процессу контейнера, тому самому из `CMD`. Нажал `Ctrl+C` — и ты отправил сигнал главному процессу, то есть остановил контейнер. Отключиться без последствий можно только последовательностью `Ctrl+P`, `Ctrl+Q`, и про неё все забывают.

Практический вывод простой: если хочешь посмотреть логи основного процесса — используй `docker logs -f`, а не `attach`. Если хочешь что-то сделать внутри — `docker exec`.

## Тонкие места, на которых легко споткнуться

- **Правки внутри контейнера не переживут пересоздание.** Поправил файл руками, а потом сделал `docker compose up -d --force-recreate` или пересобрал образ — изменения исчезли. Контейнер эфемерен by design.
- **`docker commit` — это хак, а не способ деплоя.** Закоммитить работающий контейнер в образ можно (`docker commit <id> myimage:fix`), и иногда это спасает в 3 часа ночи. Но правильный путь — поправить `Dockerfile`/конфиг и пересобрать, иначе через неделю никто (включая тебя) не вспомнит, что за магия зашита в этот образ.
- **Для постоянных правок конфигов есть volume и bind-mount.** Если файл нужно менять регулярно — пробрасывай его через `volumes` в `docker-compose.yml`, тогда он живёт на хосте и переживает любые пересборки.
- **Нет bash — не паникуй.** Минимальные образы (Alpine, distroless) идут без `bash`, иногда вообще без шелла. Для Alpine — `sh`, в distroless заходить нечем, отлаживай через `docker logs` и пересборку.

На этом все!
